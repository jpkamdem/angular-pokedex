import { PokemonBorderDirective } from './pokemon-border.directive';

describe('PokemonBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonBorderDirective();
    expect(directive).toBeTruthy();
  });
});
